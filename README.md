SpaceShare Admin Dashboard

feat(auth): implement authentication foundation and shared frontend architecture

- Configure TanStack Query with a singleton QueryClient provider
- Add global design tokens matching the Figma design system
- Implement authentication services, types, Zod schemas, and mock data
- Build reusable AuthLeftPanel branding component
- Implement Login, Forgot Password, and Reset Password flows
- Add shared constants for navigation, statuses, and theme colors
- Add common utility functions and reusable hooks
- Configure root layout, metadata, fonts, and application providers